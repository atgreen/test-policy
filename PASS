# A pass is a pass
{ "result": "PASS" }

# JUnit calls them something else -- with spelling error!
{ "report": "junit", "result": "sucess" }

# Tripwire calls them something else
{ "report": "tripwire-pdf", "status": "Passed" }
